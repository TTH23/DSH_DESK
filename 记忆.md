# DSH Desk 协作记忆（会话间持久规则）

## 推送规则（最高优先级）
- **只有用户明确说"push"才允许 push**。没有指令，一律只 commit 本地。
- 小修改只 commit；完整新功能 / 一批修复也要**先问用户**"这批要推吗"，得到同意才推。
- 推之前说明本次推送包含哪些提交。

## 标题
- 窗口标题**直接采用 DSH 页面自身格式**（"<会话名> — DeepSeek Harness"）。
- 不拦截、不转换、不用 IPC 上报标题（避免多写入者导致标题跳动）。

## 通知
- 任务完成通知：标题"任务完成"，第二行 = 智能体**回复内容**开头（前 40 字符），
  提取时跳过 token 统计行（"输入 X tok · 输出 Y tok"）与思考指示（role=status）。
- 出错通知：第二行 = 报错原因（消息首行，最长 90 字符）。
- 排队/插话队列挂起时不发"任务完成"；切换会话不误报（跟踪器绑定会话 + 连续两次确认）。

## 主题/配色
- 跟随模式：window（不持久化）/ workspace / session（按标题记忆，持久化）。
- 工作区识别：侧边栏工作区行 aria-label 以"工作区"或"Workspace actions"开头
  （会话行是"会话"前缀，勿混）；行被 HoverCard <span> 包裹，需在分组容器内查找。
- 主题色覆盖：CSS 变量定义在 body / body[data-ds-dark-theme] 上（不是 :root），
  覆盖必须用 body 目标 + !important；1.2s 重放防重置。
- 多窗口：模式/颜色变化需广播 theme-sync 到所有窗口。
- 调色板按钮锚定：贴左边缘 + 固定底部偏移（76px），不跟随设置键垂直位置（防跳动）。

## 托盘
- 二级菜单（服务/用量/通知）；用量数据在主菜单（纯数字，两行：余额 / 负号消费）。
- 状态图标角标：在线绿点 / 出错红点 / 离线灰化（gen-icon.mjs 生成变体）。

## 调试
- 工作区解析失败会上报诊断到 %APPDATA%\DSH Desk\logs\diag.log。
- 通知尝试记录在 logs\notify.log。
